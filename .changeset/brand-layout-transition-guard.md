---
"@workspace/web": patch
---

Keep the brand layout on its loading skeleton during route transitions instead of crashing to "Something went wrong" when sidebar links are built before the brand data is committed.
