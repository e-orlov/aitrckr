---
"@workspace/lib": patch
"@workspace/web": patch
---

"Add Multiple" now reports a repeat of a prompt that did not fit under the prompt limit as a duplicate instead of counting it as a second prompt over the limit.
