---
"@workspace/lib": patch
"@workspace/web": patch
---

Bulk-add prompts with tags: in "Add Multiple", write a prompt's tags after it separated by semicolons (`Prompt text;tag1;tag2`) and they land on that prompt's row before saving.
