---
"@workspace/web": patch
"@workspace/lib": patch
"@workspace/worker": patch
"@workspace/api-spec": patch
---

Saving competitor settings now keeps each competitor's identity: edits update the existing entry and removed competitors are hidden instead of deleted, so historical analytics stay attached to the right competitor.
