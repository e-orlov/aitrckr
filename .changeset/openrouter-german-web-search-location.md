---
"@workspace/lib": patch
---

Bias OpenRouter web-search results toward Germany with a fixed DE user location, sent through OpenRouter's `openrouter:web_search` server tool instead of the deprecated `:online` suffix.
