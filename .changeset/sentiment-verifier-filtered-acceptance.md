---
"@workspace/lib": patch
"@workspace/worker": patch
---

Sentiment: a verifier objection that names only aspect claims drops those claims and keeps the verified verdict; already-parked cases of that shape can be settled without a new provider call; an insufficient-credits refusal is retried instead of parked; repair prompts explain each refusal.
