---
"@workspace/lib": patch
"@workspace/worker": patch
---

Sentiment analyses can complete again: the independent verification request now uses a schema the provider accepts, and every structured-output request is checked against the provider's schema rules before it is sent.
